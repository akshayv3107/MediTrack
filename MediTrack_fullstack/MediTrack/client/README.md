
# MediTrack Client (React + Vite)

Simple dashboard for the MediTrack Flask API.

## Setup
```bash
cd client
npm install
npm run dev
# ensure the Flask API is running on http://127.0.0.1:5000 or set VITE_API_BASE
```
